// Grid-Maze Designer

// Global Constants
const NUM_ROWS = 10;
const NUM_COLS = 10;

// Create an array to represent a grid
let grid = createGridArray();

// Creat divs to model the grid array
createDivGrid(grid);

